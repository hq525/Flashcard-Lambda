# FSRS Implementation Plan

> **For agentic workers:** use superpowers:subagent-driven-development. Work in parallel on the independent file scopes below, then review integration.

**Goal:** Deliver the approved adaptive review flow across Go and React.
**Architecture:** Pure FSRS adapter, transactional review persistence, review service/API, and a browser session queue using server previews/state.
**Tech Stack:** Go, DynamoDB, official go-fsrs, React, TanStack Query, Vitest.
**Spec:** `docs/superpowers/specs/2026-09-17-fsrs-design.md`.

## Global constraints

- Preserve pre-existing edits to backend .gitignore and frontend StudyPage.tsx/StudyPage.test.tsx (image zoom).
- No deploy, remote writes, commit, push, or replacement of user work.
- Use the exact shared models/API contract in the spec. Changes to shared interfaces must be coordinated with the root agent.
- Author behavior tests before implementation and observe failures. Test real code, using doubles only at external boundaries.

### Task 1: Scheduler and models

Files: internal/models/db.go, internal/models/review.go, internal/scheduling/*, go.mod, go.sum.
Interfaces: spec's model definitions and pure scheduling functions.
- [x] Write scheduler tests for migration, all ratings, relearning, elapsed time, invalid state and preview agreement; run to observe missing implementation.
- [x] Pin official FSRS dependency and implement models/adapter, no network or persistence in scheduler.
- [x] Run `go test ./internal/scheduling ./internal/models`.

### Task 2: Transactional review storage

Files: internal/persistence/reviews.go, internal/persistence/reviews_test.go.
Interfaces: spec's ReviewStore, CardSchedule and CardReview.
- [x] Write protocol-level DynamoDB tests using SDK HTTP transport or a narrow injectable client. Verify conditionally atomic state+history and consistent reads.
- [x] Implement the store with an immutable review key scoped to card and request, revision guards, GSI history filtering/pagination, deletion and error propagation.
- [x] Run `go test ./internal/persistence`.

### Task 3: Study interface

Repository: ../flashcard-frontend. Files: src/api/{types,resources,hooks}.ts, src/features/study/*, relevant test fixtures/handlers.
Interfaces: spec's HTTP contracts. Do not run FSRS in the browser.
- [x] Update behavior tests to describe four labelled ratings and server interval previews; observe failure.
- [x] Implement API integration and session queue with due-time waiting/relearning, early finish, stable retry ID, conflict refresh, and preserved zoom/flip behavior.
- [x] Run focused tests then `npm test` and `npm run build`.

### Task 4: Service, API, integration and docs (root agent)

Files: internal/service/reviews*, internal/httpapi/reviews*, internal/httpapi/router.go, internal/app/app.go, internal/service/cascade*, internal/persistence/entities*, README.md, template.yaml if permissions require it.
- [x] Add service/API tests for duplicate retry, stale revision, validation, 404, errors, history and preview; observe failure.
- [x] Wire service/store and routes, prevent card edits corrupting scheduling, clean up history with cascade, document migration/API/deployment ordering.
- [x] Review all task diffs against the spec; fix defects with covering tests.
- [x] Run full Go tests/vet and frontend tests/build. Report verified behavior and deployment boundary.

## Progress / decisions

- Design and implementation authorized by the user; no further design approval needed.
- Use existing working directories so the approved study improvements include and preserve the user's current image-zoom edits. No git operations are needed for delivery.
- Migration is lazy, preserving legacy due dates until review and recording only genuine future review events.
- Pinned FSRS v4.0.0 requires Go 1.26; module and README updated. Existing installed Go 1.26.2 supports it.
- Card content updates use conditional schedule-exists/absent writes to protect the first-review migration race.
- Internal history predecessor links and a deletion marker make review cleanup independent of GSI lag and retryable without a table scan.
- Backend final checks passed after all fixes: `go test -race ./...`, `go vet ./...`, Linux arm64 Lambda cross-build (artifact under /tmp).
- Frontend final checks passed: 102 tests in 15 files and TypeScript/Vite production build. Root independently reran these checks successfully.
- Generic update/delete now require repository entity type, protecting immutable review records from wrong-resource IDs. Regression tests pass; independent scoped re-review found no unresolved actionable issues.
- Delivered as working-directory changes across both repositories. No commits, deployment or production data writes.
